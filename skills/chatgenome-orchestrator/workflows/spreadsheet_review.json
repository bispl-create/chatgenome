{
  "name": "spreadsheet_review",
  "description": "Run the default cohort-style workbook review workflow on the active spreadsheet source.",
  "source_type": "spreadsheet",
  "requested_view": "cohort_browser",
  "response_kind": "spreadsheet_chat",
  "studio": {
    "renderer": "cohort_browser"
  },
  "answer_template": "The {workflow_name} workflow was rerun on the active spreadsheet source.\n\n- Workflow: `{workflow_name}`\n- Active file: `{active_file}`\n- Logged tools: {logged_tools}\n- Sheets: {sheet_count}\n- Selected sheet: {selected_sheet}\n\nThe cohort browser state has been refreshed. Open the Studio card to inspect sheet-level cohort summaries and preview rows.",
  "requires": ["source_spreadsheet_path"],
  "steps": [
    {
      "tool": "cohort_sheet_browser_tool",
      "bind": "analysis"
    }
  ]
}
