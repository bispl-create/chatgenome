{
  "name": "plink_execution_tool",
  "description": "Run local PLINK 2 QC commands on a VCF.",
  "task": "variant-qc",
  "modality": "genomics",
  "approval_required": false,
  "source": "plugin",
  "routing": {
    "trigger_keywords": ["plink", "plink2"],
    "execution_intents": ["run", "execute", "qc", "실행", "돌려"],
    "requires_source_vcf": true,
    "question_argument": "none",
    "result_slot": "plink_result",
    "execution_mode": "direct"
  },
  "orchestration": {
    "tool_kind": "execution",
    "default_stage": "post-annotation",
    "depends_on": ["annotation_tool"],
    "consumes": ["vcf_path"],
    "produces": ["plink_result"],
    "priority": 80,
    "on_demand": true
  },
  "runtime": {
    "host_compatible": ["cpu"],
    "requires_gpu": false,
    "allow_cpu_fallback": true,
    "estimated_runtime_sec": 10
  },
  "help": {
    "summary": "Open the curated PLINK 2 QC workflow for the active VCF source.",
    "modes": [
      {
        "name": "qc",
        "description": "Prepare the PLINK Studio card for frequency, missingness, and Hardy-Weinberg summaries."
      }
    ],
    "options": [
      {
        "name": "mode",
        "type": "string",
        "required": false,
        "default": "qc",
        "description": "Workflow mode. The current ChatGenome build supports the qc preset."
      },
      {
        "name": "freq",
        "type": "boolean",
        "required": false,
        "default": true,
        "description": "Run allele frequency summary."
      },
      {
        "name": "missing",
        "type": "boolean",
        "required": false,
        "default": true,
        "description": "Run sample and variant missingness summary."
      },
      {
        "name": "hardy",
        "type": "boolean",
        "required": false,
        "default": true,
        "description": "Run Hardy-Weinberg summary."
      },
      {
        "name": "allow_extra_chr",
        "type": "boolean",
        "required": false,
        "default": true,
        "description": "Allow non-canonical chromosome labels when preparing the run."
      }
    ],
    "examples": [
      "@plink help",
      "@plink",
      "@plink mode=qc"
    ],
    "notes": [
      "Uses the active VCF source by default.",
      "The current ChatGenome flow opens the PLINK Studio card, where the final QC configuration can be reviewed before execution."
    ]
  }
}
